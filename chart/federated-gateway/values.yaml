# Access to the federated gateway can be requested by OpenFaaS customers
# Email us using your current channels for more.

image: ghcr.io/openfaasltd/federated-gateway:0.0.1
replicas: 1

# OpenFaaS gateway URL
gatewayURL: http://gateway.openfaas:8080

# OIDC Issuer to federate with
issuer: https://keycloak.example/realms/openfaas

# OIDC Audience to verify on tokens
audience: customer1-openfaas.example.com

logs:
  # Log debug messages
  debug: false
  # Set the log format, supports console or json
  format: "console"

nameOverride: ""
fullnameOverride: ""

resources:
  requests:
    memory: "64Mi"
    cpu: "100m"
  # limits:
  #   memory: "256Mi"
  #   cpu: "100m"

nodeSelector: {}

tolerations: []

affinity: {}
